<% if (topHit != undefined) { %>
    <h3>Top Hit</h3>

    <div onclick="openPage('<%= topHit.page %>')" page="<%= topHit.page %>" class="searchItem searchTopHit selected">
        <p><%= topHit.name %></p>
    
        <p class="searchDescription"><%- topHit.description %></p>
    </div>
<% } %>

<% if (inCV.length != 0) { %>
    <h3>in the year <%= searchTerm %> I did</h3>

    <% inCV.forEach(function(item, index) { %>
        <div onclick="openPage('<%= item.page %>')" page="<%= item.page %>" class="searchItem searchTopHit selected">
            <p><%= item.name %></p>
        
            <p class="searchDescription"><%- item.description %></p>
        </div>
    <% });  %> 
<% } %>

<% search.forEach(function(item) { %>
    <% if (item.data.length > 0) { %>
        <h3><%= item.name %></h3>
        <% item.data.forEach(function(item, index) { %>
            <div class="searchItem">
                <p onclick="openPage('<%= item.item.page %>')"><%= item.item.name %></p>
            </div>
        <% });  %> 
    <% } %>
<%  }); %>

<% if (emptySearch) { %>
    
    <div class="searchNoResult">
        <p>Sorry, but your search has no results</p>
    
    </div>
<% } %>

<div class="mobileWrapper"></div>