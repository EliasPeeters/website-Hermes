<h3>Search Suggestions</h3>
<% suggestions.forEach(function(item, index) { %>
    <div class="searchItem">
        <p onclick="document.getElementById('searchBar').value = '<%= item.name %>'; inputSend()"><%= item.name %></p>
    </div>
<% });  %> 

